app:
  port:
cors:
  origins:
db:
  host:
  port:
  username:
  password:
  database:
  connect_timeout:

jwt:
  access_token_key:
  verify:
    issuer:
    audience:

redis:
  host: '127.0.0.1'
  port: 6379
  pass:
# 缓存配置
cache:
  # 业务配置缓存
  biz:
    ttl: 600
    max: 100000
# 基于 cron 表达式的任务调度
cron:
  enabled: false
  sync_user_profile: '45 * * * * *'
# Swagger 相关配置
swagger:
  # 是否开放Swagger API Endpoint。如在生产环境不开放
  enabled: true
